[project]
name = "ca-sentinel"
version = "1.0.0"
description = "Modular contract-address change monitor with Telegram alerts"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
